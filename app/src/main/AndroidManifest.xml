giot<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="code.fortomorrow.animallover">
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimalLover">
        <activity android:name=".DiseaseActivity"></activity>
        <activity android:name=".FirstAidActivity" />
        <activity android:name=".SingleMedicineActivity" />
        <activity android:name=".SingleFoodsActivity" />
        <activity android:name=".MyprofileActivity" />
        <activity android:name=".MedicinePetActivity" />
        <activity android:name=".MyPetActivity" />
        <activity android:name=".HealthActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".BlogsActivity" />
        <activity android:name=".ProfessionalsActivity" />
        <activity android:name=".AdoptPetActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>